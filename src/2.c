#include <stdio.h>
#include <stdlib.h>

int main() {
    int x = 5;
    int y = 10;
    if (x > y) {
        printf("x is greater than y\n");
    } else {
        printf("x is less than or equal to y\n");
    }
    return 0;
}
